if comments
    each comment in comments
        li(class="list-group-item d-flex justify-content-between align-items-center")
            p(class="card-text")= comment.content
            button(class="btn btn-danger", commentId=comment._id, photoId=photo._id) Supprimer
else
    p(class="list-group-item  mb-5") Aucun commentaire pour le moment.